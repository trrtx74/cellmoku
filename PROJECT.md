# Cellmoku - React
변형 룰 오목인 Cellmoku (개척 오목)을 React로 구현한 웹 앱.

SPA로 구현, 정적 페이지로 배포 예정
react-icons
styled-components
zustand

# 페이지 구성
1. 메인 메뉴
- CPU와 대결
- 2인 대결
2. 게임 화면
- CPU 대결의 경우, 난이도 하수/중수/고수 선택

# 상단바
1. 홈으로 (홈에서는 비활성화)
2. 도움말: 모달 표시
3. 전적: CPU 난이도별로, 플레이어가 선공인 경우의 승/패/무, 후공인 경우의 승/패/무 표시
4. 언어 토글 (ko/en)


# 게임 룰
Cellmoku는 보드가 동적으로 확장되는 변형 룰 오목입니다. 플레이어는 말을 두는 것뿐만 아니라, 보드(칸)를 전략적으로 확장하여 승리 조건을 달성해야 합니다.
## 시작 상태
가운데가 비어 있는 3 x 3 형태의 보드(총 8칸)에서 시작.
## 기본 흐름 
1. 말 배치: 현재 존재하는 칸(Cell) 위에 자신의 말을 배치. 
2. 칸 획득: 방금 둔 말에 인접한(대각선 포함 8방향) '자신의 말' 개수만큼 추가 칸을 획득.
3. 칸 배치 (서브 턴): 획득한 칸을 즉시 기존 칸에 인접하게 배치. (획득한 칸은 해당 턴에 무조건 모두 배치해야함) 여기서 인접이란 한 변 이상이 접하는 것을 의미하고, 같은 턴에 배치한 칸에 접하게 이어서 배치하는 것도 가능함.
## 승리 조건
가로, 세로, 대각선 중 한 방향으로 자신의 말 5개를 먼저 연결. 6개 이상이어도 승리로 판정.
## 판정 시점 
말을 두는 즉시 승패 판정. (칸 배치는 승패에 직접 관여하지 않음)
## 보드 크기
이론상 보드는 무한히 커질 수 있지만, 기존 오목의 15 x 15 보드를 확장 가능 경계로 정한다. (에이전트 또한 해당 조건으로 훈련됨)


# 게임 보드 UI
- 말과 칸은 div로 처리, 그 외 div로 불가능한 요소는 svg로 구현
- 말은 원형. 선공이 흑, 후공이 백
- 칸은 border radius 적용된 사각형. 초기 칸이 아닌 플레이어가 배치한 칸의 좌상단에는 배치한 플레이어의 색상과 동일한 작은 tag를 표시
- 칸 grid는 옅은 색으로, 15x15

- 다시 시작 (단축키 R, 확인창 뜸), 되돌리기 버튼 (단축키 Z)
- 되돌리기 버튼: CPU 대결에서는 내 마지막 수로 되돌림, 2인 대결에서는 직전 턴의 마지막 수로 되돌림
- 되돌리기를 위해서 store에 대국 기록을 모두 저장 (보드 상태, 취한 행동)

- 조작을 위한 cursor (배치 표시자) 구현
- 말 또는 칸을 옅은 색으로 표시
- 말의 경우, 획득하는 칸 개수를 확인할 수 있도록 cursor 에 인접한 자신의 말에 연결되는 band가 있고, 화살표 모양 무늬가 cursor 방향으로 흐르도록 애니메이션 구현
- 칸 배치 단계에서는 배치 가능한 위치를 다른 색의 옅은 칸으로 표시
- 데스크탑, 모바일, 키보드 조작 별도 구현

## 데스크탑
- 마우스 hover하면 cursor가 해당 칸으로 이동, 칸 획득 indicator도 마찬가지로 실시간 업데이트
- 클릭하면 바로 말 또는 칸 배치

## 모바일
- 한 번 터치하면 cursor가 해당 칸으로 이동
- 같은 칸에 한 번 더 터치하면 실제로 말 또는 칸 배치

## 키보드
- 방향키 또는 WASD로 cursor 이동
- Enter 또는 Space로 말 또는 칸 배치


# CPU 에이전트
## 신경망 추론
- **경로**: PyTorch 체크포인트 → **ONNX 변환** → `onnxruntime-web`.
- **백엔드**: WASM(CPU, 호환성 최상)을 기본, **WebGPU/WebGL**을 가능 시 fallback.
- 128채널 8블록의 1회 forward는 브라우저에서 수십 ms 수준 예상 → 대국용 충분.

## MCTS + threat/VCF 엔진 (가장 큰 작업)
- 파이썬 MCTS·VCF·환경 로직을 **브라우저 구동 언어로 포팅**.
  - 1순위: **TypeScript 포팅**(단순·디버깅 용이, 대국용 sim 수면 속도 충분).
  - 필요 시: Rust/C++ → WASM(대개 불필요할 것으로 예상).
- **⚠️ 재현 정확성이 핵심 리스크**: cell 순열 등가, 관점 부호, threat 판정의
  칸 지형 실측, frontier 증분 등 파이썬에서 단위 테스트로 못 박은 불변식을
- CPU 난이도는 MCTS sims와 temp 수치로 조절. 개발 중에 사용할 수 있도록 동적으로 이 수치를 변경시키는 디버깅 설정 구현 필요

## 모델 로딩
- **양자화**: fp32 → int8/fp16으로 30MB → **10~15MB급**. 대국 강도 손실은 MCTS가
  흡수해 대개 미미.
- **캐시**: Service Worker / 브라우저 캐시로 첫 로드 후 재방문 즉시.
- CPU 대전을 선택하면 동적으로 모델을 로드. 플레이어가 첫 턴을 고민하는 동안 로드될 수 있도록


# 로깅
게임 종료 또는 다시시작 시 SUPABASE에 게임 기록을 로깅한다.